# Copy to .env.local and set your MongoDB connection string
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>/<database>?retryWrites=true&w=majority

# Optional: if deploying behind a proxy or non-root URL, set base URL for SSR fetches
# NEXT_PUBLIC_BASE_URL=http://localhost:3000
